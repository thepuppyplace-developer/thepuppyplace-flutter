import 'package:flutter/material.dart';

class BoardListTile extends StatelessWidget {
  const BoardListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: ListTile(

      ),
    );
  }
}
